// Components barrel — re-exports app components for easier imports.
// These re-exports duplicate the original files and are intended to
// simplify migration. Remove duplicates after consolidating imports.
export { default as ChatSidebar } from '../../components/ChatSidebar';
export { default as IncomingCall } from '../../components/IncomingCall';
export { default as MessageInput } from '../../components/MessageInput';
export { default as MessageList } from '../../components/MessageList';
export { default as VideoCall } from '../../components/VideoCall';
